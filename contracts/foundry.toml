[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
solc_version = "0.8.28"
optimizer = true
optimizer_runs = 200
via_ir = true
fuzz = { runs = 512 }
invariant = { runs = 64, depth = 32 }

# Prototypes only. No RPC endpoints, no deployment scripts, no keys.
# Deployment is gated: see ../docs/protocol/deployment-gates.md
