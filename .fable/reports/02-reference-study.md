# 02 — Reference study (The Archivist / Art Director)

Full study: `docs/reference/creativeglu-design-study.md`. Raw captures (gitignored, never committed): `artifacts/reference/creativeglu/` — 26 PNGs + structural probe JSON + capture notes.

## Coverage

- Pages: `/home`, `/ai-transformation`, `/portfolio`, `/contact-us`
- Viewports: 1440×1000, 1728×1117, 1024×768, 768×1024, 390×844, 360×800 (full matrix on home; desktop+mobile on secondary pages)
- Captured: above-fold + full-page per viewport, menu-open state, structural probe (computed styles, font metrics, element counts), console logs per page.

## Key measurements

Near-black lab(3,−1.5,1.2) ground with film grain; Inter-only typography at extreme scale (H1 ≈ 109px/700 → body 20px/300/1.75); 10 background videos + 1 canvas on home; ~9,083px page height; floating pill nav; full-screen typographic menu; editorial two-column sections; blueprint-style card stack; logo wall; quote carousel; FAQ accordion; giant bordered final CTA. Zero console errors on home at all viewports (portfolio ships CORS-broken S3 images — a defect we did not replicate).

## Originality measures

Adopted as *principles only*: cinematic confidence, extreme type contrast, full-screen menu-as-moment, declaration+evidence sections, restrained motion, terminal mega-CTA. Explicitly not used: their words, videos, orb/ribbon renders, grain texture, pill nav, rainbow glyphs, Inter-only identity, client logos, quotes, section order. NO END STATE's grammar — open frames, ledger ruling, append-only strata, registry rows, semantic signal spectrum, serif italic declarations — appears nowhere on the reference site.
